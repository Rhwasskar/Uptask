<!DOCTYPE html>
html(lang="en")
	head
		meta(charset="UTF-8")
		meta(name="viewport", content="width=devidce-width, initial-scale=1.0")
		meta(http-equiv="X-UA-Compatible", content="ie-edge")
		link(rel="stylesheet", href="/css/normalize.css")
		link(rel="stylesheet", href="/css/app.css")
		title #{nombrePagina}
	body
		header.barra
			h1 UpTask - Administrador de Proyectos
			a(href="#") Cerrar Sesión
			
		main.contenedor
			aside.contenedor-proyectos
				.panel.crear-proyecto
					a(href="/nuevo-proyecto" class="boton" fontsize="8" ) Nuevo_Proyecto
						span +
				.panel.lista-proyectos
					h2 Proyectos
					ul#proyectos.proyectos
						if proyectos
							each proyecto in proyectos
								li
									a(href=`/proyectos/${proyecto.url}`)= proyecto.nombre
									//- pre= Object.keys(vardump(proyecto))
						
						


			block contenido

			//- pre= vardump(proyectos)
		script(src="/dist/bundle.js")